# requirements.txt — SCRM Streamlit Dashboard
#
# Pin exact versions to ensure Streamlit Community Cloud
# builds identically every time. If you hit a dependency conflict,
# the most likely culprit is numpy — try downgrading to 1.24.0.
#
# TensorFlow is intentionally excluded. LSTM forecasts are
# pre-computed and loaded from lstm_forecasts.csv at repo root.

streamlit==1.32.0
pandas==2.1.0
numpy==1.26.0
plotly==5.19.0
scikit-learn==1.4.0
joblib==1.3.0
