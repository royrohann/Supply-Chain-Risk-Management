# requirements.txt — SCRM Streamlit Dashboard
#
# Versions updated to have pre-built wheels for Python 3.14+.
# Using >= minimum bounds (not ==) so pip resolves the latest
# compatible wheel rather than falling back to source compilation.
#
# TensorFlow is intentionally excluded — LSTM forecasts are
# pre-computed and loaded from lstm_forecasts.csv at repo root.

streamlit>=1.40.0
pandas>=2.2.3
numpy>=2.0.0
plotly>=5.19.0
scikit-learn>=1.5.0
joblib>=1.4.0
